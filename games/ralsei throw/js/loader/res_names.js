var tsl_texture; // The Samsara Lodge logo

var susie_walk_textures = Array(4); // Susie walk cycle.
var susie_grab_texture;
var susie_throw_textures = Array(3); // Throw animation.

var ralsei_stand_texture;
var ralsei_throw_texture;
var ralsei_head_texture;

var kris_texture;
var kris_hug_texture;

var kround_texture;
var kround_kick_textures = Array(5);

var hammer_texture;
var hammer_whack_textures = Array(9);

var letter_textures = Array(20); // Textures for each of the letters in "deltarune  ralsei hwrot"

var aim_arrow_texture; // the texture for picking a direction to fire Ralsei in

var background_tile_texture; // the fallback texture for backgrounds. Moves at 1:10 parallax.
var foreground_tile_texture; // the fallback texture for foregrounds. Moves at 1:1 parallax.
var ground_texture; // texture for the ground. Moves at 1:1 parallax.